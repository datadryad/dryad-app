default: &default
  repository:
    type: merritt
    endpoint: "http://merritt-dev.cdlib.org/object/update"
    username: "dataone_dash_submitter"
    password: "test02"
    profile: "demo_open_context_content"
  contact_email: ["contact1@contact.com", "contact2@contact.com"]
  abbreviation: "LBNL"
  short_name: "LBNL"
  long_name: "Livermore National Lab"
  landing_page: ".lbl.gov"
  domain_regex: "datasets-dev.lbl.gov$"
  tenant_id: lbnl
  logo: dash_lbl_logo.png
  authentication:
    strategy: shibboleth
    entity_id: urn:mace:incommon:lbl.gov
    entity_domain: datasets-dev.lbl.gov

development:
  <<: *default
  #Add any items that need to override the defaults here

stage:
  <<: *default
  #Add any items that need to override the defaults here

production:
  <<: *default
  #Add any items that need to override the defaults here

test:
  <<: *default
  #Add any items that need to override the defaults here
