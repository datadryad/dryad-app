$("#js-keywords__container").replaceWith('<%= raw escape_javascript(render partial: "stash_datacite/subjects/keywords_list", locals: {subjects: @subjects, resource_id: @resource.id }) %>');
loadSubjects();
<% unless params[:subject].blank? %>
  $('#keyword').focus();
<% else %>
  $('#description_methods').focus();
<% end %>