<% # local: curation_activity %>
<div class="o-admin-dialog">
  <%= form_for :curation_activity,
        url: curation_status_change_path(curation_activity.resource.identifier_id),
        method: :post,
        remote: true do |f| %>

    <div class="c-input">
      <label class="c-input__label">Status</label>
      <%= f.select :status, options_for_select(filter_status_select(curation_activity.status)), include_blank: true %>
    </div>
    <div class="c-input">
      <label class="c-input__label">Notes</label>
      <%= f.text_area :note, class: 'c-input__textarea' %>
    </div>
    <%= f.hidden_field :resource_id %>
    <%= submit_tag 'Submit' %>
    <%= button_tag 'Cancel', type: 'button', id: 'cancel_dialog' %>

  <% end %>

</div>
