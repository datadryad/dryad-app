module StashEngine
  describe CurationStats do

    before(:each) do
      ### set up an array with 3 identifiers, which each have 3 resources
    end

    describe :datasets_curated do
      it 'knows when there are none' do
      end

      it 'counts correctly when there are some' do
        ### set up two CAs, one that has a correct transition and one that doesn't
      end
    end

  end
end
