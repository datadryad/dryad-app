document.getElementById('chart-size-chart').innerHTML = ''
document.getElementById('chart-size-chart').append(document.createRange().createContextualFragment("<%= escape_javascript(render partial: 'size_chart') %>"));
document.getElementById('chart-size-chart').setAttribute('aria-busy', false)
if (!Array.from(document.querySelector('#search_charts div').children).some(d => d.getAttribute('aria-busy') == 'true')) {
  document.getElementById('search_charts').setAttribute('aria-busy', false)
}