$('.remove_record').bind('ajax:success', function() {
  $(this).closest('form').remove();
  $('.add_orcid_link').replaceWith('<%= j render partial: "stash_datacite/creators/add_orcid_link", locals: { resource: @resource } %>')
});