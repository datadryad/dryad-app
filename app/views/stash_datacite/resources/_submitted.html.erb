<table>
  <%= render 'submitted_head' %>
  <tbody>
    <%= render partial: 'submitted_line', collection: @in_progress_lines, as: :line %>
  </tbody>
</table>
<%= paginate @resources, params: {page_size: @page_size}, remote: true %>