var resource_id = '<%= params[:id] %>';
if(window.location.pathname.startsWith("/stash/dataset")) {
  var title = "<%= escape_javascript(@resource.titles.try(:first).try(:title)) %>";
  document.title = title;
}
$('#display_resource').html("<%= escape_javascript(render(partial: 'show')) %>");
loadReviewMap(resource_id);
