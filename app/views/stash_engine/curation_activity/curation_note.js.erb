document.getElementById('genericModalDialog').close();

$('#curation_table_<%= @resource.id %> tr:last').after("<%= escape_javascript(
  render partial: 'stash_engine/admin_datasets/curation_activity_row', locals: { curation_activity: @resource.curation_activities.last })
%>");