$('.js-download').prop('disabled', false);

$('#genericModalContent').html("<%= escape_javascript(render partial: 'download_timeout_message') %>");

$(function() {
    $('#genericModalDialog')[0].showModal();

    $("#cancel_dialog").click(function (e) {
        e.preventDefault();
        $('#genericModalDialog')[0].close();
    });
});


