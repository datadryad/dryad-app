$('#not_uploaded_file_<%= @temp_id%>').replaceWith("<%= escape_javascript(render(partial: 'stash_engine/file_uploads/uploaded_file', object: @my_file)) %>")

if($('.upload-it:first').length){
  $('.upload-it:first').click();
}else{
  $('#upload_all').hide();
}
