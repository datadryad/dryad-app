<h1>Welcome to Stash Engine</h1>
<p>Not much going on here.</p>

<!-- this is something like /Shibboleth.sso/Login?target=https://#{host}/auth/shibboleth/callback -->

<!--
def self.login_path_with_entity(host, entity)
  "https://#{host}/Shibboleth.sso/Login?target=#{self.return_target(host)}&entityID=#{self.shibboleth_entity_id(entity)}"
end

# host would be dash2-dev.cdlib.org, for example.
# entity would be like urn:mace:incommon:ucop.edu  or https://idp.calstatela.edu/idp/shibboleth or urn:mace:incommon:ucla.edu

# self.return_target(host) is -> CGI.escape("https://#{host}/auth/shibboleth/callback")
# self.shibboleth_entity_id(entity) is -> CGI.escape(entity)


<p>
  <a href="https://dash2-dev.ucop.edu/Shibboleth.sso/Login?target=https%3A%2F%2Fdash2-dev.ucop.edu%2Fstash%2Fauth%2Fshibboleth%2Fcallback&entityID=urn%3Amace%3Aincommon%3Aucop.edu">UCOP Login</a>
</p>

<p>
  <a href="/auth/developer">Developer login</a>
</p>

-->
