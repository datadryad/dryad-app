<p>This is a test page for logging in without ORCID support.</p>

<p>
  To log in as an existing user, be sure the ORCID you enter matches an existing one.  Otherwise for new ORCIDs, a new
  user will be created.
</p>

<%= form_with(url: stash_url_helpers.test_login_path, method: :post, local: true) do %>
  <p>
    <%= label_tag(:first_name, 'First name') %><br/>
    <%= text_field_tag(:first_name) %>
  </p>

  <p>
    <%= label_tag(:last_name, 'Last name') %><br/>
    <%= text_field_tag(:last_name) %>
  </p>

  <p>
    <%= label_tag(:email, 'Email') %><br/>
    <%= text_field_tag(:email) %>
  </p>

  <p>
    <%= label_tag(:tenant_id, 'Partner') %><br/>
    <%= select_tag :tenant_id, options_from_collection_for_select(@tenants, "id", "name"), class: '.o-select' %>
  </p>

  <p>
    <%= label_tag(:role, 'Role') %><br/>
    <%= select_tag :role, options_for_select([ ['User', 'user'],
                                               ['Admin', 'admin'],
                                               ['Limited Curator', 'limited_curator'],
                                               ['Curator', 'curator'],
                                               ['Superuser', 'superuser'] ]), class: '.o-select' %>
  </p>

  <p>
    <%= label_tag(:orcid, 'Orcid') %>*<br/>
    <%= text_field_tag(:orcid) %>
  </p>

  <p>
    <%= submit_tag("Log In") %>
  </p>

  <p>ORCIDs are normally 16 digits and with a dash between every 4 digits, but this is not enforced with this login.</p>
<% end %>
