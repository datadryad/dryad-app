$(".sharing_form").html("<%= escape_javascript(render partial: 'stash_engine/shares/form',
  locals: { share: @share, resource_id: @share.identifier.last_submitted_resource, path: stash_url_helpers.shares_update_path}) %>")
