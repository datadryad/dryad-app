// open a modal dialog
document.getElementById('genericModalContent').innerHTML = "<%= escape_javascript(render partial: 'curation_form') %>";
ReactRailsUJS.mountComponents()
document.getElementById('genericModalDialog').showModal();
document.getElementById('cancel_dialog').removeEventListener('click', cancelClick);
document.getElementById('cancel_dialog').addEventListener('click', cancelClick);
<% if @field == 'curation_activity' %>
  initDatePicker();
  document.getElementById('stash_engine_resource_curation_activity_status').removeEventListener('change', statusChange)
  document.getElementById('stash_engine_resource_curation_activity_status').addEventListener('change', statusChange)
<% end %>
