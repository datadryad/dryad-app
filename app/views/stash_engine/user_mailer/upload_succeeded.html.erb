<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type'/>
</head>
<body>

<p>Dear <%= @vars[:to_name]%>:</p>

<p>Your dataset &ldquo;<%= @vars[:title]%>&rdquo;
  (<%= link_to("https://dx.doi.org/#{@vars[:identifier].identifier}", "doi:#{@vars[:identifier].identifier}")%>)
  has been successfully submitted to Dash.</p>

<p>To see the status of your dataset, visit the <%= link_to 'My Datasets', "#{@vars[:my_datasets_uri]}" %> page.
  There may be a brief delay for processing before the dataset is published.</p>

<p>Please do not reply to this email.</p>

</body>
</html>
