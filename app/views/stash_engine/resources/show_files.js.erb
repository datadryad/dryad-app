// refresh the file lists by ajax

// redraw standard files upload table
$('#upload_list').html("<%= escape_javascript(render partial: 'stash_engine/data_files/files_upload_line',
          locals: {resource: @resource, uploads: @uploads } ) %>");

// redraw manifest table
var show10 = $('#show_10_files').is(":visible");
var showAll = $('#show_all_files').is(":visible");

$('#validated_files').html("<%= escape_javascript(
          render partial: 'stash_engine/data_files/validated_files', locals: {resource: @resource}) %>");

// set link states again to what they were for few/many files
tableStateRestorer(show10, showAll);

