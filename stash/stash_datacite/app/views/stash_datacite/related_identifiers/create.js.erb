// redraw this form

<% form_id = unique_form_id(@related_identifier) %>

var form = $('#<%= "replace_#{params[:form_id]}" %>');

form.replaceWith("<%= escape_javascript(render partial: "stash_datacite/related_identifiers/form", locals: {
  related_identifier: @related_identifier, path: stash_datacite.related_identifiers_update_path }) %>");
